<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.fanqie.homework.submit.mapper.BSubmitdetailMapper">

    <select id="selectsubmitdetaillist" resultType="cn.fanqie.homework.submit.entity.BSubmitdetail">
    SELECT *
    FROM
    b_submitdetail
    WHERE b_submitdetail.submit_id = #{submitId}
    </select>
</mapper>
